[build]
  # Install locked deps, then CRA production bundle → publish/
  command = "npm ci && npm run build"
  publish = "build"

# Single Page App: all routes → index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
